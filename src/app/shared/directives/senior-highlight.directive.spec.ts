import { SeniorHighlightDirective } from './senior-highlight.directive';

describe('SeniorHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SeniorHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
